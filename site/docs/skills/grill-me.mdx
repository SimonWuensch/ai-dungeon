---
sidebar_position: 19
---

import SkillHeader from '@site/src/components/skills/SkillHeader';
import CompareColumns from '@site/src/components/diagrams/CompareColumns';

# grill-me

<SkillHeader category="Standalone" invocation="user" githubPath="productivity/grill-me" />

**Dasselbe rücksichtslose Interview wie `grill-with-docs` — nur ohne Papierspur.** Ruft intern nur `grilling` auf, speichert aber nichts lokal und legt kein `CONTEXT.md` an. Der richtige Skill, wenn es gar kein Arbeitsverzeichnis gibt, in dem sich etwas festhalten ließe.

## Wann nutzen

- Du schärfst einen Plan, ein Design oder einen Text, aber **arbeitest nicht in einem Repo** — kein Ort, an dem eine Papierspur überhaupt hinginge.
- Bist du in einem Arbeitsverzeichnis, ist `grill-with-docs` **strikt die bessere Wahl**: gleicher Loop, plus Gedächtnis.

**Wie oft?** Matt Pococks eigene Empfehlung: *"Use them every time you want to make a change"* — auch bei kleinen. Der Reflex sollte lauten "erst grillen, dann bauen", nicht nur bei großen Features.

## Stateless vs. stateful

<CompareColumns
  eyebrow="grill-me vs. grill-with-docs"
  left={{
    heading: 'grill-me',
    items: [
      'Kein Arbeitsverzeichnis nötig',
      'Speichert nichts — reine Konversation',
      'Für Pläne, Designs, Texte ohne Repo-Bezug',
    ],
  }}
  right={{
    heading: 'grill-with-docs',
    focal: true,
    items: [
      'Setzt ein Arbeitsverzeichnis voraus',
      'Schreibt live in CONTEXT.md und ADRs',
      'Strikt vorzuziehen, wenn ein Repo da ist',
    ],
  }}
/>

## Beispiel

Bevor überhaupt ein Repo existiert, sitzt ein Product Owner mit Claude an einem Notiz-Dokument und will die Idee "Nutzer verwalten ihre Benachrichtigungen selbst" grob vorschärfen — welche Kanäle, welche Digest-Frequenzen sind überhaupt sinnvoll? `/grill-me` interviewt dazu, eine Frage nach der anderen, mit Empfehlung. Am Ende steht eine geklärte Idee — aber nichts davon ist gespeichert. Sobald ein Repo dazukommt, übernimmt an genau dieser Stelle `grill-with-docs` und hält ab da alles fest.

## Gut zu wissen

Beide rufen denselben Mechanismus auf: `grilling` — Runden, eine Frontier aus offenen Fragen, Fakten sind Aufgabe des Agenten, Entscheidungen bleiben immer bei dir.
